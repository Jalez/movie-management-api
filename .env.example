# Database Configuration
DB_PASSWORD=moviepass
POSTGRES_PASSWORD=moviepass
POSTGRES_DB=moviedb
POSTGRES_USER=movieuser

# Optional JVM Configuration (has defaults)
JAVA_OPTS=-Xms512m -Xmx1024m -XX:+UseG1GC -XX:+HeapDumpOnOutOfMemoryError

# Optional Server Configuration (has defaults)
SERVER_TOMCAT_MAX_THREADS=200
SERVER_TOMCAT_MIN_SPARE_THREADS=20